default namespace = "http://www.arin.net/regrws/messages/v1"

# NOTE:  when adding additional files to this directory, you must add them to RelaxNgSchemaHelper.supplements

start = element createdDate { dateTime }
dateTime = xsd:string