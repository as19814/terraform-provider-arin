# Sandbox implementation coverage

Objective: implement all resources and data sources supported in ARIN's OT&E
sandbox. This file tracks remaining work; a passing test for one API family does
not establish coverage of another. Production mutations are outside testing scope.

The inventory starts with the official snapshots in `arin-api/README.md`. Each
family requires schemas, validated client behavior, Terraform lifecycle/import
where applicable, generated examples/docs, mock tests, and sandbox evidence.
Unsupported operations need documented, reproducible OT&E evidence rather than
being assumed out of scope. Reconcile this inventory against live documentation
again before declaring full coverage.

| API family | Current provider coverage | Remaining work / evidence |
| --- | --- | --- |
| IRR AS sets | Simple XML resource and individual/list data sources; mock and OT&E lifecycle pass | Simple and advanced lifecycles pass OT&E; endpoint/field audit complete, including MNT/ANY membership-by-reference and computed POC metadata; see [audit](irr-simple.md) |
| IRR route/route6 | Simple XML resource and individual/list data sources; IPv4/IPv6 OT&E lifecycle includes multiple route-set memberships, replacement and clearing; individual data source exposes member_of | Advanced RPSL resource/data-source lifecycle passes IPv4/IPv6 OT&E; scoped linked-route metadata/deletion client passes IPv4/IPv6 OT&E; arin_irr_route_metadata lifecycle and owner graph pass mocks and IPv4/IPv6 OT&E with membership and restoration; arin_irr_linked_route import/update/independent deletion passes IPv4/IPv6 OT&E; simple endpoint/field audit complete, including direct POST/PUT evidence for generated network and POC fields and native IPv4/IPv6 child, parent-only, downstream and organization route-list semantics; see [linked-route evidence](irr-route-metadata.md); see [route evidence](irr-routes.md) |
| IRR route sets | Simple XML resource plus individual/list data sources; mock and OT&E lifecycle pass | Simple and advanced lifecycles pass OT&E; endpoint/field audit complete, including IPv4/IPv6 members and computed POC metadata; see [audit](irr-simple.md) |
| IRR aut-num | Simple XML resource plus individual/list data sources; all six policies and membership pass OT&E | Simple and advanced lifecycles pass OT&E; endpoint/field audit complete for six policies, membership and computed POC metadata; see [audit](irr-simple.md) |
| NET records | Read data sources, arin_net and arin_net_metadata; IPv4/IPv6 single/multi-block lifecycles, metadata and POC editing pass OT&E; pending recovery passes mocks | Remove-with-message client and resource policy pass mocks; empty-message and exactly two approved message/attachment removals pass IPv4/IPv6 OT&E client tests with verified cleanup and replay prevention; native Terraform correspondence and live asynchronous ticket evidence remain; see [evidence](net-registration.md) |
| Customers | Individual data source and managed recipient resource; standalone and combined customer/NET Terraform lifecycles pass mocks and IPv4/IPv6 OT&E, including import, recipient replacement, reused NET handles and cleanup | Customer CRUD and payload audit complete, including computed country code3/e164 metadata and an OT&E country change; see [graph evidence](customer-network.md) |
| Organizations | Individual data source, arin_org and arin_org_poc; full Terraform CRUD/import/recovery pass mocks; no-change PUT and all non-admin association roles pass OT&E; live creation returned a pending review ticket | Computed POC role descriptions and read-only pending recovery pass OT&E; await live creation review, then validate disposable mutable fields, Admin replacement and deletion; no automatic handle discovery from ticket-only responses; see [organization evidence](organizations.md) and [association evidence](org-pocs.md) |
| POCs | Individual/reference data sources, arin_poc, arin_poc_email and arin_poc_phone; full and individual-contact lifecycles pass OT&E | Managed POC endpoint/payload audit complete, including phone descriptions, nested-set planning and country metadata; see [evidence](pocs.md) |
| DNS delegations | Read data sources, arin_delegation and arin_delegation_nameserver; full-zone and individual-NS lifecycles pass IPv4/IPv6 OT&E | Delegation endpoint/payload audit complete, including computed DS display names and IPv4/IPv6 algorithm/digest changes; see [evidence](delegations.md) |
| Hosted ROAs | Individual/list data sources, arin_roa and shared transaction client; Terraform create/import/update/destroy, AS0 and IRR link/unlink/relink pass OT&E; non-default maximum lengths and both deletion modes pass client tests; uncertain-write recovery passes mocks and Terraform | Endpoint/payload audit recorded; atomic bundle planner/recovery engine passes mocks and native combined writes; Terraform bundle create/import/combined changes/destroy passes OT&E with restoration; recovery passes mocks; scoped linked-route metadata/deletion client passes OT&E; Terraform metadata lifecycle and owner graph pass IPv4/IPv6 OT&E with membership and restoration; independent linked-route Terraform deletion passes IPv4/IPv6 OT&E while preserving ROA authorization; manual route adoption/unlink metadata and duplicate-prefix rejection verified; AS0 auto-link silently normalizes and is rejected locally; see [evidence](rpki.md) |
| Hosted ASPAs | Individual/list data sources and arin_aspa; atomic provider replacement, import, AS0-only declaration and deletion pass Terraform OT&E; original inventories restored | Endpoint/payload audit recorded; client bundle planner/recovery is verified; Terraform [managed atomic bundle](rpki-bundle.md) lifecycle passes OT&E with restoration; recovery passes mocks; see [evidence](rpki.md) |
| Tickets | Detail, summary, list, summaries, message and attachment data sources; typed metadata, guarded close client and arin_ticket_status; closure/recovery pass mocks and closed-ticket lifecycle passes OT&E without writes | Append-only message client and arin_ticket_message receipt resource pass mocks, including attachment import, expiry, state-only destroy and uncertain-submission recovery; native correspondence, full-ticket status PUT with byte-preserved metadata and Terraform update_method passes mocks; successful live RESOLVED-to-CLOSED transitions and asynchronous reconciliation remain; see [report/ticket evidence](reports-tickets.md) |
| Reports | Client and arin_report_request for all four types; create/import/refresh/destroy and recovery without duplicate submissions pass mocks; associations resource lifecycle passes OT&E; client associations/reassignment auto-close; WhoWas ASN/NET returned missing-access 401 | Native WhoWas completion requires account access; report endpoint inventory and associations attachment integration audit recorded with all six ticket read operations, full/reference payloads and byte comparisons; see [evidence](reports-tickets.md) |
| Public RDAP | Org network discovery, ASN record/inventory, POC references, full public entity lookup and handle/name searches, address/prefix network lookup, and reverse-domain registrations including DS data and hierarchy searches, plus network/ASN handle/name and entity reverse searches, IPv4/IPv6 network hierarchy searches, exact nameserver-to-domain searches and help metadata; complete registration JSON is exposed; IPv4/IPv6 Terraform reads pass OT&E and production without credentials | RDAP field/endpoint reconciliation recorded; ASN hierarchy, standalone nameserver operations and unsupported domain searches return native 501; see [RDAP audit](rdap.md) |
| Whois-RWS | Six individual record, twelve relationship, five search and three IP/CIDR data sources with typed fields and complete XML; native Terraform reads and clean plans pass on both origins | POC type/status/phone descriptions pass native reads; documented middle_name is exposed in individual/search/relationship outputs with populated mock and native absent-value coverage; delegation DS algorithm/digest names now pass populated native lookup tests on both origins and mock relationship tests; final field/endpoint audit and delegation-search ambiguity remain, with a reproducible bounded probe; showDetails, showPocs and showARIN are implemented and pass native tests; see [Whois-RWS evidence](whois-rws.md) |
| Authenticated downloads | Streaming client for Bulk Whois selectors/formats and invalid-POC ZIPs passes mocks, including bounded large downloads and credential-safe failures; native query-authenticated GETs return 403 | arin_bulk_whois and arin_invalid_pocs data sources have mock coverage for bounded content/metadata reads; user confirms no sandbox download approval; successful native Terraform reads and content validation remain; see [download audit](authenticated-downloads.md) |
| Delegated RPKI and publication | arin_rpki_certificate now exposes create/update, validated refresh, key replacement and destroy with synthetic Terraform lifecycle tests; import and full recovery remain unfinished; arin_rpki_provisioning now reads parent resource classes and certificates with signed client and Terraform nested-state tests; arin_rpki_publication now reads signed RFC 8181 inventories using file-backed keys and persistent journals, with signed client tests and Terraform acceptance mapping/state tests; native delegated verification remains unavailable; protocol/enrollment requirements audited; arin_rpki_setup parses all four local setup message types with BPKI self-signature checks, bounded input and Terraform acceptance coverage | arin_rpki_setup_request generates child/publisher XML from an existing CA certificate with round-trip and Terraform tests; private CMS profile decoding and RSA signature checks have unit/OpenSSL coverage; private explicit-anchor path and CRL validation plus caller-supplied signing-time checks pass synthetic tests; local CMS request signing with existing EE identities passes unit and OpenSSL chain/CRL verification; private durable timestamp/pending-request journal passes reopen, cross-process lock and crash tests; private signed request evidence is now durably retained before dispatch with restart, digest-binding and failed-mutation tests; private signed HTTP transport now passes fake-server tests with durable dispatch, CMS verification, response-validator gating and retry prevention; private RFC 8181 inventory client now validates list replies and authenticated errors with signed fake-server coverage; list has no nonce and equal-time replay remains possible; arin_rpki_publication_bundle now exposes atomic owned-object lifecycle with Terraform drift/preservation tests; changed bundles now guard unchanged members against concurrent edits with signed atomic-rejection coverage; exact-content manifest import now has Terraform import/clean-plan and rejection tests; pending publication intent now has peer-bound signed-request inspection and before/after inventory comparison with ambiguity/conflict tests; authenticated recovery inventory reads now hold the original lease and use separate digest-scoped journals with signing-time floors and signed rejection tests; private publication reconciliation now atomically records an explicitly selected matching inventory outcome and advances watermarks, with signed commit and persistence-failure tests; publication recovery CLI now exposes observation and explicit matching-outcome commit with private configuration, mode-validation and signed API tests; pending revocation intent now has signed peer/handle/class/key binding and malformed-request tests without clearing pending state; private revocation recovery now reads signed inventory under the original lock with digest-scoped journals and key/class absence classification, preserving pending state; revocation observation now has a CLI and client API with strict private provisioning configuration and report/mode tests; file-backed issuance and revocation client APIs now expose anchored RRDP validation and exact issued-certificate identity with signed tests; issuance/revocation recovery and native verification remain unfinished; private publication batches now support create/replace/withdraw with hash preconditions, validated errors and signed fake-repository lifecycle tests; private up-down revocation now checks handles/class/key and preserves pending state for scheduled outcomes with signed fake-server coverage; private up-down inventory now parses classes/resource sets/certificates with signed fake-server tests; private issue client now verifies CSR signatures and returned key/class/echoed requests/issuer signatures/dates with signed fake-server tests; issuance now requires valid CA SIA and exact preservation of requested publication locations; CA CSR algorithm/attribute/extension preflight now has signed-request tests; private original-profile resource-extension decoder now has AS/IPv4/IPv6/inheritance and malformed-range tests; private resource-path containment/inheritance now rejects overclaims and authorization gaps in unit tests; private exact resource-path verification now combines configured-anchor PKIX/CRL checks with resource containment in signed-chain tests; resource-path CRL version/algorithm/extension/entry checks now reject signed out-of-profile CRLs; RFC 9829 correction removes resource CRL number ranking and rejects ambiguous candidates; resource name/DER time checks are integrated with boundary and signed-certificate tests; core CA certificate extension/algorithm checks now run in path validation with signed rejection tests; private manifest content/file-set parsing now checks DER, filenames, hashes and freshness with unit coverage; manifest CMS signature/profile decoding now has signed rejection and OpenSSL coverage while retaining provisioning profile isolation; manifest decoding now enforces the EE certificate profile, inherited resources and signedObject SIA with signed rejection tests; combined manifest checks now bind a supplied issuer, publication locations and hash-selected CRL with EE revocation tests; private manifest-backed path verification now walks from the explicit anchor using listed child certificates and CRLDP-bound CRLs with signed three-level tests; private durable manifest-path validation now records per-anchor/issuer/URI versions atomically and rejects rollback with reopen/corruption tests; private issuance now has a manifest-backed resource-path gate before journal completion with signed fake-server retry-prevention tests; issued allocation/request intersection matching now runs before history commit with AS/IPv4/IPv6 and signed fake-server mismatch tests; bounded RRDP snapshot decoding now checks digest/session/serial, XML and object limits with malformed/boundary tests; bounded notification decoding now validates snapshot references and contiguous sorted delta sequences with large-serial and malformed-input tests; private HTTPS notification/snapshot retrieval now has conditional GET, bounded reads, timeout/redirect and TLS fake-server coverage; private atomic delta application now verifies repository/session/next-serial and old hashes with HTTPS, failure-isolation and buffer-ownership tests; private refresh coordination now prefers complete delta chains, falls back to snapshots and retains prior data with TLS lifecycle/polling tests; private persistent RRDP caching now records attempts before dispatch and retains data on failure with restart, concurrency and corruption tests; private supplied-path RRDP retrieval now assembles scoped manifest publications with signed TLS and durable path-validation tests; private issuance now connects explicit-chain RRDP retrieval to anchored validation and history before completion, with signed end-to-end fake-server tests; explicit read-only journal recovery now preserves watermarks and records abandoned requests with reopen, mutation-rejection and signed retry tests; automatic chain discovery, anchor rotation/history recovery, updated-profile audit, native interoperability, recovery and provider integration remain; ownership plan recorded; signed protocol clients/resources and native evidence remain; user confirms no delegated sandbox enrollment or identity; see [audit](delegated-rpki.md) |

## Confirmed sandbox behavior

- AS-set POC links are generated by ARIN. Attempts to configure changed links
  return E_ENTITY_VALIDATION. They are computed attributes.
- Empty remarks elements cause HTTP 500. Omitting the element clears remarks.
- IRR route reads include autoLinkedRoaHandle for linked objects. The ordinary
  route resource rejects those objects; dedicated metadata and imported linked-route
  resources support scoped changes and independent deletion.
- mostSpecificNet requires an existing exact start/end range. Parent NET lookup
  is used to verify the registration covering a disposable host route.
- Both IPv4 /32 and IPv6 /128 disposable route lifecycles passed in OT&E.
- Route-set XML accepts the `^+` range operator in OT&E despite the web UI guide
  saying range operators are unsupported. Collection clearing and MNT membership
  round-trip successfully.
- IRR aut-num supports writable AS name, all six routing policy fields and
  membership in AS sets. Creation, changes, clearing, import and deletion passed
  without changing the underlying ASN registration.
- Customer records support creation, address/name/privacy updates, comment clearing,
  import and deletion. Updates preserve server-generated identity and registration
  date. Creation does not itself reassign a network.
- IPv4 /32 and IPv6 /64 NET assignments pass Terraform simple reassignment,
  detailed reassignment, reallocation, metadata update/clearing and deletion.
  Tested responses contain completed NET records without tickets. Asynchronous
  ticket responses and persisted recovery state are covered by mocks and Terraform
  acceptance tests. NET Origin AS was retired in July 2025 and OT&E silently
  discards it on create and update; it is not exposed as a writable field.
- Multi-block IPv4 and IPv6 NET lifecycles pass Terraform creation, update,
  import, clean-plan and deletion checks for all three assignment modes.
- Existing-NET metadata supports omission/preservation and explicit clearing.
  OT&E accepts T, N and AB POC roles, and rejects R and D despite their presence
  in the generic payload table. The direct-allocation metadata test submits
  unchanged values and verifies the complete record remains identical.
- Delegation writes require the immutable name and TTL namespace on DS TTL
  elements. Single-nameserver POST resets omitted TTL to inheritance; full PUT
  preserves omitted TTLs on existing nameservers and DS records. New records
  may inherit TTL. Client and Terraform lifecycles pass IPv4/IPv6 sandbox tests
  with exact restoration of prior configuration.
- OT&E keys may differ from production because snapshots refresh monthly.

## Verification commands

- `make check`: vet, race tests, fake-server acceptance tests and binary build.
- `make generate`: reproducible provider documentation and examples.
- `ARIN_TEST_ORG_HANDLE=... make testlive`: explicitly read-only live tests.
- `ARIN_TEST_ORG_HANDLE=... make testote`: opt-in OT&E mutations on disposable objects; DNS and organization association tests snapshot and restore.

Credentials, live account responses, state and saved plans must not be committed.
