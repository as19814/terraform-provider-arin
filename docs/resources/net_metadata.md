---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "arin_net_metadata Resource - arin"
subcategory: ""
description: |-
  Manage name, comments and POC links on an existing NET, including direct allocations. This resource never creates or deletes address-space registrations. Destroy leaves the last metadata in ARIN and removes Terraform management. Omitted fields preserve current values; use explicit empty collections to clear them. Do not configure overlapping fields for the same NET in multiple resources. When combining with arin_net, configure only POC links here. Import by NET handle.
---

# arin_net_metadata (Resource)

Manage name, comments and POC links on an existing NET, including direct allocations. This resource never creates or deletes address-space registrations. Destroy leaves the last metadata in ARIN and removes Terraform management. Omitted fields preserve current values; use explicit empty collections to clear them. Do not configure overlapping fields for the same NET in multiple resources. When combining with arin_net, configure only POC links here. Import by NET handle.

## Example Usage

```terraform
# The NET must already exist. This also supports direct allocations.
# Destroy stops managing metadata and leaves the network and its values intact.
resource "arin_net_metadata" "example" {
  handle   = "NET-192-0-2-0-1"
  name     = "EXAMPLE-NET"
  comments = ["Network operations: https://example.net"]

  poc_links = [
    {
      handle   = "TECH-EXAMPLE"
      function = "T"
    },
    {
      handle   = "ABUSE-EXAMPLE"
      function = "AB"
    },
  ]
}

# Omit name, comments or poc_links to preserve their current values.
# Use comments = [] or poc_links = [] to clear a collection.
# With arin_net, configure only poc_links here to avoid overlapping ownership.
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `handle` (String) NET registration to manage. It must already exist.

### Optional

- `comments` (List of String) Ordered operational comments. Omission preserves current comments; an empty list clears them.
- `name` (String) Network name. Omission preserves the current value.
- `poc_links` (Attributes Set) Explicit NET POC associations. Omission preserves existing associations; an empty set clears them. Organization-inherited contacts are separate. NETs accept Tech, NOC and Abuse POCs only. (see [below for nested schema](#nestedatt--poc_links))

### Read-Only

- `id` (String) Existing NET handle.

<a id="nestedatt--poc_links"></a>
### Nested Schema for `poc_links`

Required:

- `function` (String) One of AB (Abuse), N (NOC) or T (Tech).
- `handle` (String) POC handle.

Read-Only:

- `description` (String) POC role description returned by ARIN.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import arin_net_metadata.example NET-192-0-2-0-1
```
