---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "arin_irr_rpsl Resource - arin"
subcategory: ""
description: |-
  Manage one advanced ARIN IRR object through RPSL. Supports route, route6, as-set, route-set and aut-num. Owns the complete policy payload; ARIN validates routing-policy syntax and contact authorization. Identity and organization changes require replacement. Alignment, attribute-name ordering, folded continuation lines and server timestamps do not cause drift; repeated attribute ordering and policy values remain significant. Import existing advanced objects before managing them. Destroy deletes the IRR object, not its underlying network or ASN registration. Do not manage the same object through an XML resource.
---

# arin_irr_rpsl (Resource)

Manage one advanced ARIN IRR object through RPSL. Supports route, route6, as-set, route-set and aut-num. Owns the complete policy payload; ARIN validates routing-policy syntax and contact authorization. Identity and organization changes require replacement. Alignment, attribute-name ordering, folded continuation lines and server timestamps do not cause drift; repeated attribute ordering and policy values remain significant. Import existing advanced objects before managing them. Destroy deletes the IRR object, not its underlying network or ASN registration. Do not manage the same object through an XML resource.

## Example Usage

```terraform
# Use contacts registered to your organization, and evaluate writes in OT&E.
resource "arin_irr_rpsl" "example" {
  object_type = "as-set"
  name        = "AS-EXAMPLE"
  org_handle  = "EXAMPLE-1"
  rpsl        = <<-RPSL
    as-set: AS-EXAMPLE
    descr: Example advanced AS set
    admin-c: ADMIN-ARIN
    tech-c: TECH-ARIN
    mnt-by: MNT-EXAMPLE-1
    members: AS64496
    source: ARIN
  RPSL
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Canonical prefix, uppercase set name or AS<number>, matching the first RPSL attribute.
- `object_type` (String) route, route6, as-set, route-set or aut-num.
- `org_handle` (String) Maintaining organization. The RPSL mnt-by must be MNT- followed by this handle.
- `rpsl` (String) Complete single-object RPSL payload. Include identity, source: ARIN, mnt-by and the required contact/policy attributes. Server-created timestamps are ignored in comparisons. Configured formatting is retained when the remote policy matches.

### Optional

- `origin_as` (String) Required for routes; empty for other objects. Must match the origin attribute.

### Read-Only

- `id` (String) Import identity: type/name, with ,AS<number> appended for routes.
- `pending_creation` (Boolean) True after an uncertain creation. Blocks deletion and replacement until the object is reconciled and imported.
- `remote_rpsl` (String) Complete latest server representation, including ARIN formatting and timestamps.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import arin_irr_rpsl.example as-set/AS-EXAMPLE
# Route example: terraform import arin_irr_rpsl.example 'route6/2001:db8::/48,AS64496'
```
